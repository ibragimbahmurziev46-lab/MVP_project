# Конфликт Git

Конфликт возник в файле `api-plan.md` между ветками `main` и `feature/auth`.
Обе ветки изменяли строку версии API в одном и том же месте файла.

Разрешён вручную: оставлена комбинированная строка `Версия API: main + auth`.
Коммит: "исправление: разрешён конфликт в api-plan между main и feature/auth".